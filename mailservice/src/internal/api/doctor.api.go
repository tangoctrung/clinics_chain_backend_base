package api

import (
	"github.com/gin-gonic/gin"
)

func (s *mailservice) DoctorEndPoint(g *gin.RouterGroup) {
	//userGroup := g.Group("/patient")

	// userGroup.GET("/", s..HandleGet)
	// userGroup.POST("/", s.User.HandlePost)
}
